import { renderPagePlaceholder } from "../components/Layout.js";

export function TweetFeedPage() {
  return renderPagePlaceholder(
    "Community Feed",
    "Tweet timeline with likes and retweets will be built in a future phase."
  );
}
