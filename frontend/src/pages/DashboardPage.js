import { renderPagePlaceholder } from "../components/Layout.js";

export function DashboardPage() {
  return renderPagePlaceholder(
    "Creator Dashboard",
    "Analytics, subscriber stats, and video management table coming in a future phase."
  );
}
