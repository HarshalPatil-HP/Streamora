import { renderPagePlaceholder } from "../components/Layout.js";

export function HomePage() {
  return renderPagePlaceholder(
    "Home Feed",
    "Video grid coming in Phase 2. This is where your curated stream of content will live."
  );
}
